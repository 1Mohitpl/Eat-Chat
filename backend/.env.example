PORT=5000
NODE_ENV=development

# MongoDB connection string
MONGO_URI=mongodb://127.0.0.1:27017/beyuumi

# JWT secret & expiry (use a strong random secret in production)
JWT_SECRET=beyuumi_super_secret_change_me_in_production
JWT_EXPIRES_IN=7d

# Rolling log files location
LOG_DIR=logs